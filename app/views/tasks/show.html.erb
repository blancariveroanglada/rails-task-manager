<h1><%= @task.title %></h1>
<ul>
  <li><%= link_to "delete", task_path(@task), method: :delete, data: { confirm: "Are you sure you want to delete #{@task.title}?" } %></li>
  <li><%= link_to "edit", edit_task_path(@task) %></li>
</ul>
